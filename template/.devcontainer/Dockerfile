# Managed by https://github.com/felixtrautwein/devenv - only edit below the marker.
#
# The universal image already contains python/uv, docker cli, gh, kubectl, helm,
# terraform, hcloud and ansible. Pick a slimmer tag (python, embedded) in
# .devcontainer/.env via DEVENV_IMAGE if this project does not need all of it.
ARG DEVENV_IMAGE=ghcr.io/felixtrautwein/devenv:web
FROM ${DEVENV_IMAGE}

# --- repository specific additions below ---
